// Fonseca Fitness — Supabase config template.
// Copy this file to assets/portal-config.js and fill in your project's values
// (Supabase dashboard → Project Settings → API).
//
// The anon (public) key is safe to publish: it only grants access allowed by
// your Row Level Security (RLS) policies. Without assets/portal-config.js the
// portal simply runs in demo mode — no real accounts, no errors.
window.FONSECA_SUPABASE = {
  url: "https://YOUR-PROJECT.supabase.co",
  anonKey: "YOUR-ANON-PUBLIC-KEY"
};
